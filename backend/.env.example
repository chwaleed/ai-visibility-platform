# --- LLM provider (required for real pipeline runs; tests run without it) ---
ANTHROPIC_API_KEY=sk-ant-...

# --- Real search data: SE Ranking Keyword Research API ---
# (free tier: 100K credits, no card; volume+difficulty in one call)
SERANKING_API_KEY=your-seranking-api-key

# --- App ---
DATABASE_URL=sqlite:///dev.db
SECRET_KEY=change-me
FLASK_ENV=development
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
RATELIMIT_ENABLED=true
